# MagSafe Dark

[English version](README.md)

[Поддержать проект](https://boosty.to/smd.monster/donate)

MagSafe Dark — приложение для строки меню macOS, которое управляет индикатором на разъёме MagSafe.

Приложение умеет выключать индикатор, возвращать управление macOS, включать зелёный или оранжевый цвет, запускать временные эффекты, работать по недельному расписанию и показывать состояние задач Codex CLI.

> MagSafe Dark использует недокументированный SMC-ключ Apple `ACLC`. Обновление macOS или прошивки может изменить или отключить эту возможность.

## Требования

- macOS 13 или новее
- MacBook на Apple Silicon с MagSafe 3

Приложение проверено на MacBook Pro M5. После установки поддержку можно проверить командой:

```bash
magsafe-dark probe
```

Ожидаемый результат:

```text
supported
```

## Основные возможности

- Полностью выключить индикатор MagSafe или вернуть обычное управление macOS.
- Включить зелёный, оранжевый цвет, одиночную индикацию или режимы мигания.
- Запустить временный режим на заданное время.
- Настроить недельное расписание по дням и времени.
- Использовать отдельные режимы для работы, успеха и ошибки Codex.
- Реагировать на подключение и отключение зарядки через системные события IOKit.
- Показывать заряд батареи, состояние зарядки и ожидаемое время завершения зарядки в строке меню.
- Выбирать значок батареи или лампочки.
- Запускать приложение автоматически при входе в систему.
- Открывать диагностику и журналы прямо из меню.

## Установка

### Установочный пакет

Собрать и проверить локальный неподписанный пакет:

```bash
zsh ./scripts/check-release.sh --package
open build
```

Пакет создаётся по пути:

```text
build/MagSafeDark-<версия>-unsigned.pkg
```

### Установка из исходников

```bash
git clone https://github.com/bulava92/magsafe-dark.git
cd magsafe-dark
zsh ./scripts/check-release.sh
zsh ./install.sh
```

Для уже загруженного репозитория:

```bash
cd ~/Projects/magsafe-dark
git pull
zsh ./scripts/check-release.sh
zsh ./install.sh
```

Во время установки macOS один раз запросит пароль администратора. После установки обычная работа приложения и команд не требует `sudo`.

Приложение устанавливается сюда:

```text
/Applications/MagSafe Dark.app
```

## Использование приложения

Откройте **MagSafe Dark** из папки «Программы». В меню можно:

- выбрать режим индикатора;
- запустить или отменить таймер;
- настроить недельное расписание;
- настроить индикацию Codex;
- включить запуск при входе;
- выбрать значок состояния и стиль иконки;
- включить отображение процента батареи, подключения питания и завершения зарядки;
- открыть диагностику и журналы;
- проверить наличие обновлений.

Горячие клавиши при открытом меню:

- `⌘⇧0` — переключить «Выключено» / «Штатный режим»;
- `⌘⇧G` — зелёный;
- `⌘⇧O` — оранжевый;
- `⌘Q` — выход.

## Режимы индикатора

| Режим | Что делает |
|---|---|
| Штатный | индикатором управляет macOS |
| Выключено | индикатор не горит |
| Зелёный | постоянный зелёный цвет |
| Оранжевый | постоянный оранжевый цвет |
| Одиночная индикация | короткая одиночная индикация |
| Медленное мигание | медленное мигание оранжевым |
| Быстрое мигание | быстрое мигание оранжевым |
| Мигание и выключение | мигание с последующим выключением |

## Таймеры

Таймер временно заменяет обычный режим индикатора. После завершения MagSafe Dark заново вычисляет актуальное состояние, а не восстанавливает устаревшее значение.

```bash
magsafe-dark for 900 off
magsafe-dark timer-status
magsafe-dark cancel-timer
```

## Недельное расписание

Откройте пункт **«Настроить расписание…»**. Для каждого интервала можно выбрать дни недели, время начала, время окончания и режим индикатора. Интервалы могут переходить через полночь.

Стандартный отключённый шаблон:

```text
Каждый день  08:00–23:00  Штатный режим
Каждый день  23:00–08:00  Выключено
```

При включении расписания текущий режим применяется сразу. Уже активный таймер или индикация Codex сначала завершаются. Ручной выбор при включённом расписании действует до следующей границы расписания.

Изменения источника питания отслеживаются через IOKit. При подключении внешнего питания и активном расписании текущий режим применяется сразу и повторяется через короткую задержку для страховки.

## Индикация Codex CLI

MagSafe Dark может показывать через индикатор MagSafe состояние Codex CLI:

- идёт работа;
- задача завершилась успешно;
- задача завершилась с ошибкой.

Запускайте Codex через обёртку:

```bash
codex-led
codex-led exec "Fix the failing tests"
```

Можно запускать несколько задач Codex одновременно. Рабочая индикация остаётся активной, пока работает хотя бы одна задача.

## Приоритет режимов

```text
1. Пользовательский таймер
2. Активная индикация Codex
3. Ручной режим до следующей границы расписания
4. Текущий интервал расписания
5. Постоянный ручной режим
6. Обычный режим macOS
```

## Управление через Terminal

Прямое управление:

```bash
magsafe-dark off
magsafe-dark system
magsafe-dark green
magsafe-dark orange
magsafe-dark flash
magsafe-dark blink-slow
magsafe-dark blink-fast
magsafe-dark blink-off
```

Расписание:

```bash
magsafe-dark schedule edit
magsafe-dark schedule status
magsafe-dark schedule enable
magsafe-dark schedule disable
magsafe-dark schedule next
```

Запуск другой команды с индикацией:

```bash
magsafe-dark run -- make test
magsafe-dark run --working blink-slow -- npm run build
magsafe-dark run --success green --error blink-fast -- ./deploy.sh
```

Код завершения запущенной команды сохраняется.

## Диагностика

```bash
magsafe-dark status
magsafe-dark settings
magsafe-dark state
magsafe-dark diagnostics
magsafe-dark log-path
```

## Решение проблем

Проверка совместимости и фоновой службы:

```bash
magsafe-dark probe
/usr/local/libexec/magsafe-led-client ping
/usr/local/libexec/magsafe-led-client status
```

Ожидаемый ответ на `ping`:

```text
pong
```

Таймер и активная индикация Codex имеют приоритет выше расписания. Если расписание не применяется, проверьте `magsafe-dark schedule status`.

## Удаление

```bash
zsh ./uninstall.sh
```

Скрипт возвращает индикатор под обычное управление macOS и удаляет MagSafe Dark, фоновые службы, расписание, настройки и журналы.

## Конфиденциальность и безопасность

MagSafe Dark работает локально. Системная фоновая служба принимает только фиксированный набор команд управления индикатором и не выполняет произвольные команды оболочки. После установки обычная работа приложения и CLI не требует `sudo`.

## Лицензия

[MIT](LICENSE)
